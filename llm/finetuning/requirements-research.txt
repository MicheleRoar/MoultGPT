# MoultGPT — llm/finetuning/ research requirements
#
# The main llm/requirements.txt is intentionally torch-free (the live
# backend only calls remote LLM APIs — see that file's header comment).
# This file is for the *research* side only: fine-tuning a local base
# model with LoRA/QLoRA, evaluating it, and serving it locally.
#
# Install with:
#   pip install -r llm/finetuning/requirements-research.txt

# Core training stack
torch>=2.3.0
transformers>=4.42.0
datasets>=2.19.0
accelerate>=0.31.0
peft>=0.11.0
bitsandbytes>=0.43.0            # 4-bit / QLoRA loading

# Preference optimization (train_dpo.py) — API has moved across trl
# versions, see that script's module docstring before upgrading blindly.
trl>=0.9.0

# Distributed / multi-GPU (used via `accelerate launch`, see accelerate_config.yaml)
deepspeed>=0.14.0; sys_platform == "linux"

# Fast local inference / serving of the fine-tuned adapter
vllm>=0.5.0; sys_platform == "linux"

# Experiment tracking (opt in via `--tracking wandb|mlflow`)
wandb>=0.17.0
mlflow>=2.14.0

# Research code quality / reproducibility
pytest>=8.2.0
